<%= turbo_frame_tag "search-results" do %>
  <div class="space-y-6">
    <!-- メインキーワード分析 -->
    <div class="bg-white shadow rounded-lg overflow-hidden">
      <div class="px-4 py-5 border-b border-gray-200 sm:px-6">
        <h3 class="text-lg leading-6 font-medium text-gray-900">
          "<%= results[:main_keyword][:keyword] %>"
        </h3>
      </div>
      <div class="p-6">
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
          <div class="text-center p-4 bg-gray-50 rounded-lg">
            <dt class="text-sm font-medium text-gray-500">検索ボリューム</dt>
            <dd class="mt-1 text-2xl font-semibold text-gray-900">
              <%= number_with_delimiter(results[:main_keyword][:search_volume]) %>
            </dd>
            <dd class="text-xs text-gray-500">月間</dd>
          </div>
          <div class="text-center p-4 bg-gray-50 rounded-lg">
            <dt class="text-sm font-medium text-gray-500">難易度</dt>
            <dd class="mt-1">
              <span class="inline-flex items-center px-3 py-1 rounded-full text-lg font-semibold
                <%= case results[:main_keyword][:difficulty][:color]
                    when 'green' then 'bg-green-100 text-green-800'
                    when 'yellow' then 'bg-yellow-100 text-yellow-800'
                    when 'red' then 'bg-red-100 text-red-800'
                    end %>">
                <%= results[:main_keyword][:difficulty][:score] %>
              </span>
            </dd>
            <dd class="text-xs text-gray-500"><%= results[:main_keyword][:difficulty][:level] %></dd>
          </div>
          <div class="text-center p-4 bg-gray-50 rounded-lg">
            <dt class="text-sm font-medium text-gray-500">CPC</dt>
            <dd class="mt-1 text-2xl font-semibold text-gray-900">
              ¥<%= results[:main_keyword][:cpc] %>
            </dd>
            <dd class="text-xs text-gray-500">平均</dd>
          </div>
          <div class="text-center p-4 bg-gray-50 rounded-lg">
            <dt class="text-sm font-medium text-gray-500">トレンド</dt>
            <dd class="mt-1 text-2xl font-semibold <%= results[:main_keyword][:trend] >= 0 ? 'text-green-600' : 'text-red-600' %>">
              <%= results[:main_keyword][:trend] >= 0 ? '+' : '' %><%= results[:main_keyword][:trend] %>%
            </dd>
            <dd class="text-xs text-gray-500">前年比</dd>
          </div>
        </div>

        <% if site %>
          <div class="mt-4 flex justify-end">
            <%= button_to "#{site.name}に追加", site_keywords_path(site, keyword: { word: results[:main_keyword][:keyword] }),
                method: :post,
                class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-teal-600 hover:bg-teal-700" %>
          </div>
        <% end %>
      </div>
    </div>

    <!-- 関連キーワード -->
    <% if results[:related_keywords].any? %>
      <div class="bg-white shadow rounded-lg overflow-hidden">
        <div class="px-4 py-5 border-b border-gray-200 sm:px-6">
          <h3 class="text-lg leading-6 font-medium text-gray-900">関連キーワード</h3>
        </div>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">キーワード</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ボリューム</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">難易度</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">CPC</th>
                <% if site %>
                  <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">アクション</th>
                <% end %>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% results[:related_keywords].each do |kw| %>
                <tr>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                    <%= kw[:keyword] %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    <%= number_with_delimiter(kw[:search_volume]) %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                      <%= case kw[:difficulty][:color]
                          when 'green' then 'bg-green-100 text-green-800'
                          when 'yellow' then 'bg-yellow-100 text-yellow-800'
                          when 'red' then 'bg-red-100 text-red-800'
                          end %>">
                      <%= kw[:difficulty][:level] %> (<%= kw[:difficulty][:score] %>)
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    ¥<%= kw[:cpc] %>
                  </td>
                  <% if site %>
                    <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                      <%= button_to "追加", site_keywords_path(site, keyword: { word: kw[:keyword] }),
                          method: :post,
                          class: "text-teal-600 hover:text-teal-900" %>
                    </td>
                  <% end %>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    <% end %>

    <!-- ロングテールキーワード -->
    <% if results[:long_tail_keywords].any? %>
      <div class="bg-white shadow rounded-lg overflow-hidden">
        <div class="px-4 py-5 border-b border-gray-200 sm:px-6">
          <h3 class="text-lg leading-6 font-medium text-gray-900">ロングテールキーワード</h3>
          <p class="mt-1 text-sm text-gray-500">競合が少なく、コンバージョン率が高い傾向</p>
        </div>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">キーワード</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ボリューム</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">難易度</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">CPC</th>
                <% if site %>
                  <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">アクション</th>
                <% end %>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% results[:long_tail_keywords].each do |kw| %>
                <tr>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                    <%= kw[:keyword] %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    <%= number_with_delimiter(kw[:search_volume]) %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                      <%= case kw[:difficulty][:color]
                          when 'green' then 'bg-green-100 text-green-800'
                          when 'yellow' then 'bg-yellow-100 text-yellow-800'
                          when 'red' then 'bg-red-100 text-red-800'
                          end %>">
                      <%= kw[:difficulty][:level] %> (<%= kw[:difficulty][:score] %>)
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    ¥<%= kw[:cpc] %>
                  </td>
                  <% if site %>
                    <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                      <%= button_to "追加", site_keywords_path(site, keyword: { word: kw[:keyword] }),
                          method: :post,
                          class: "text-teal-600 hover:text-teal-900" %>
                    </td>
                  <% end %>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    <% end %>
  </div>
<% end %>
